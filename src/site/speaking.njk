

{% extends 'layouts/page.njk' %}

{% block sidebar %}
  <h1>Speaking</h1>
  <p>
    I’m lucky enough to have been able to travel to speak at many conferences and events around the world. Usually talking about the hows and whys of web development.
  </p>
{% endblock %}


{% block content %}


<h2>Upcoming events</h2>
{% for item in notist.events.future %}
  <div class="blog-list-item">
    <p class="by-line">{{ item.starts_on | dateDisplay }} in {{ item.address }}</p>
    <h3><img src="/images/flags/{{ item.country }}"><a href="{{ item.url }}" target="NEW" rel="noopener">{{ item.title }}</a></h3>
  </div>
 {% endfor %}


<h2>Previous events</h2>
{% for item in notist.events.past %}
  <div class="blog-list-item">
    <p class="by-line">{{ item.starts_on | dateDisplay }} in {{ item.address }}</p>
    <h3><a href="{{ item.url }}" target="NEW" rel="noopener">{{ item.title }}</a></h3>
  </div>
 {% endfor %}


{% endblock %}


