---
layout: layouts/base.vto
title: "Hawksworx"
---

<section>
  <hgroup>
    <h1>hawksworx</h1>
    <p>Phil Hawksworth's home on the web</p>
  </hgroup>
</section>
<hr>
<section>
 <div class="listing">
  {{set post = search.pages("post").reverse()[0] }}
  <div class="list-item">
    <h4><a href="{{post.url}}">{{post.title}}</a></h4>  
    <p>{{post.subtitle}}</p>  
    {{ for tag of post.tags }}
    <a class="tag" href="blog/tags/{{tag}}">{{tag}}</a>
    {{ /for }}
  </div>
</section>

