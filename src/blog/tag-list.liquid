---
layout: base.liquid
permalink: /blog/tags/
---

<section>
  <h1>Tags</h1>

</section>
<section>
{% for tag in collections.allTags %}
  <h2>{{tag[0]}}</h2>
  {% for post in tag[1] %}
    <li><a href="{{post.url}}">{{ post.data.title }}</a></li>
  {% endfor %}
{% endfor %}
</section>
  
